(function($) {
    'use strict';

    // All JavaScript pluging initialization code here

    // WoW js init
    var wowInit = new WOW({
        boxClass: 'wow',
        animateClass: 'animated', 
        offset: 0, 
        mobile: true, 
        scrollContainer: null
    });
    wowInit.init();


})(jQuery)